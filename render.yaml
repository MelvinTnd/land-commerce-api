services:
  - type: web
    name: land-commerce-api
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: APP_NAME
        value: BéninMarket
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: false
      - key: APP_KEY
        generateValue: true
      - key: APP_URL
        fromService:
          type: web
          name: land-commerce-api
          property: host
      - key: DB_CONNECTION
        value: pgsql
      - key: DB_HOST
        fromDatabase:
          name: land-commerce-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: land-commerce-db
          property: port
      - key: DB_DATABASE
        fromDatabase:
          name: land-commerce-db
          property: database
      - key: DB_USERNAME
        fromDatabase:
          name: land-commerce-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: land-commerce-db
          property: password
      - key: SESSION_DRIVER
        value: file
      - key: CACHE_STORE
        value: file
      - key: QUEUE_CONNECTION
        value: sync
      - key: LOG_CHANNEL
        value: stderr
      - key: LOG_LEVEL
        value: error

databases:
  - name: land-commerce-db
    plan: free
    databaseName: beninmarket
